# .env.example
# Copy this file to .env and add your actual database URLs
# These environment variables are automatically set by Netlify when deployed

# Your Netlify database URLs (automatically provided by Netlify)
NETLIFY_DATABASE_URL=postgresql://username:password@host/database
NETLIFY_DATABASE_URL_UNPOOLED=postgresql://username:password@host/database

# Instructions:
# 1. These URLs are automatically set by Netlify in production
# 2. For local development, you can set them manually if needed
# 3. Run 'netlify dev' to use Netlify's local development environment